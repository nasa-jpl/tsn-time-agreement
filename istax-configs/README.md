# istax-configs

This folder contains the set of istax configs to use for each of the 4 switches that will be used in the topology. For now, these are standalone configurations that use a special static IP for each management interface. Once we have a test network set up with DHCP, these configs will move to the tsn-scenarios repo where they will be applied with base configs and templating.
